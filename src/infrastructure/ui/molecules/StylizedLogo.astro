<div class="relative group w-full h-full flex items-center justify-center">
  <!-- Glowing Background Effect (Subtle) -->
  <div class="absolute inset-0 bg-accent-blue/10 blur-[80px] rounded-full animate-pulse-slow"></div>
  
  <!-- Holographic Scanline Overlay -->
  <div class="absolute inset-0 z-20 pointer-events-none bg-[linear-gradient(rgba(18,16,16,0)_50%,rgba(0,0,0,0.25)_50%),linear-gradient(90deg,rgba(255,0,0,0.06),rgba(0,255,0,0.02),rgba(0,0,255,0.06))] bg-[length:100%_4px,3px_100%] animate-scanlines opacity-50 mix-blend-overlay rounded-full"></div>

  <!-- The Logo Image with Hologram styles -->
  <img 
    src="/images/3d/nomudevLogoVector.svg" 
    alt="NomuDev Logo" 
    class="relative z-10 w-[80%] h-auto object-contain animate-float drop-shadow-[0_0_10px_rgba(33,150,243,0.3)] opacity-90 transition-all duration-500 group-hover:scale-105 group-hover:opacity-100"
  />
</div>

<style>
  .animate-float {
    animation: float 6s ease-in-out infinite;
  }
  
  .animate-pulse-slow {
    animation: pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }

  .animate-scanlines {
      background-size: 100% 3px;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-20px); }
  }

  @keyframes pulse {
    0%, 100% { opacity: 0.1; transform: scale(0.95); }
    50% { opacity: 0.3; transform: scale(1.05); }
  }
</style>
